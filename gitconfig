[user]
	name = Eric Bouchut
	email = ebouchut@gmail.com
[color]
	ui = auto
[alias]
	hist = log --pretty=format:'%h  %ad | %s%d [%an]' --graph --date=short
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	catchup = log --patch --reverse --topo-order _ribbon..origin/master
	pullify = config add remote.origin.fetch '+refs/pull/*/head:refs/remotes/origin/pr/*'
	plog = log --graph --format='%Cgreen%h %Cred%aN%Cblue%d %Creset%s %C(yellow)(%cr)%Creset'
	logf = log --follow
	unstage = reset HEAD --
	s = status
	ctags = !.git/hooks/ctags
	pf = push --force-with-lease
	wow = log --all --graph --oneline --decorate --simplify-by-decoration
	score = shortlog -sn
	mine = !git plog --author=\"$(git config user.name) <$(git config user.email)>\"
	today = !git plog --since=midnight --author=\"$(git config user.name) <$(git config user.email)>\"
	week = !git log --oneline --graph --decorate  --since \"one week ago\" --author=\"$(git config user.name) <$(git config user.email)>\"
[apply]
	whitespace = nowarn
[merge]
	tool = splice
[core]
	autocrlf = input
	safecrlf = true
	excludesfile = ~/.gitignore_global
	pager = less -FRSX
[credential]
	helper = osxkeychain
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[push]
	default = simple
[rerere]
[dude]
	interval = 60
[commit]
[init]
	templatedir = ~/.git_template
[gitsh]
	noGreeting = true
[pager]
	log = diff-highlight | less -FRSX
	show = diff-highlight | less -FRSX
	diff = diff-highlight | less -FRSX
[diff]
	tool = vimdiff
	submodule = log
[difftool "diffmerge"]
	cmd = diffmerge \"$LOCAL\" \"$REMOTE\"
[mergetool "diffmerge"]
	cmd = "diffmerge --merge --result=\"$MERGED\" \"$LOCAL\" \"$(if test -f \"$BASE\"; then echo \"$BASE\"; else echo \"$LOCAL\"; fi)\" \"$REMOTE\""
	trustExitCode = true
[mergetool "splice"]
	cmd = vim -f \"$BASE\" \"$LOCAL\" \"$REMOTE\" \"$MERGED\" -c SpliceInit
	trustExitCode = true
[mergetool]
	bc3 = trustExitCode

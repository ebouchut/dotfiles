#!/bin/sh

#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# Configuration specific to the host eight
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

export LC_ALL=fr_FR.UTF-8

# Add rvm bin dirs to $PATH
source /etc/profile.d/rvm.sh
export PATH=$PATH:/usr/local/rvm/bin
